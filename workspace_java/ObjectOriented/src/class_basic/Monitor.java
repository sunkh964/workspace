package class_basic;

// 클래스를 구성하는 멤버(요소) - 변수, 메서드의 정의, 생성자

// 변수 : 변수를 선언한 위치에 따라 변수의 종류가 달라짐
// 지역변수 : 메서드 안에서 선언된 변수, 반드시 초기화 후 사용 가능
// 매개변수 : 메서드 선언부의 소괄호 안에서 선언된 변수
// 맴버변수(필드, 속성) : 클래스 안에서 선언된 변수,
//                     초기화 하지 않으면 기본 값으로 자동 초기화

// Monitor class : 실 생활에 존재하는 모니터를 코드로 구현하기 위한 설계도

public class Monitor {
    // 변수 - 브랜드, 가격, 색상
    String brand; //null
    int price; //0
    String color; //null


}
